// using System;

// class Program
// {
    // static void Main()
    // {
        // double[] numbers = new double[10];
        // double total = 0.0;
        // int index = 0;
        // Console.WriteLine("Enter 0 or a negative number to stop:");
        // while (true)
        // {
            // double n = double.Parse(Console.ReadLine());
            // if (n <= 0) // Break the loop if the user enters 0 or a negative number
            // {
                // break;
            // }
            // numbers[index] = n;
            // index++;
            // if (index == 10) // Break if the array size limit (10) is reached
            // {
                // break;
            // }
        // }
        // Console.WriteLine("\nNumbers entered:");
        // for (int i = 0; i < index; i++)
        // {
            // Console.WriteLine(numbers[i]);
            // total += numbers[i];
        // }
        // Console.WriteLine("\nThe total of all numbers is " +total);
    // }
// }
